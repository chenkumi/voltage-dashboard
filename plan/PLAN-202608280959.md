# Plan

修正 Smart Dashboard 在工具失敗後仍產生誤導性完成訊息的問題。核心方向是讓 provider 錯誤以真正的 AI SDK tool error 傳回 Agent、提供可修正但不洩漏資料的錯誤分類，並保留一個不可再呼叫工具的最終摘要 step，確保 Agent 能解讀最後一批工具結果。

目前已確認相關 5 個測試檔共 98 項測試通過，`npm run typecheck` 也通過；完整的 LLM 對話流程仍需使用原始 prompt 實機驗證。

> 本計畫請使用 execute-plan 技能執行。Checkbox（`- [ ]`）語法為追蹤格式。

## Scope

- In:
  - 跨 iframe tool error 正規化與傳遞。
  - SQL 安全檢查的細分類與安全錯誤訊息。
  - Report tool schema 與 argument error 的可修正性。
  - Agent tool loop 的 step lifecycle log。
  - 預留最終摘要 step，避免最後一次 tool result 無人解讀。
  - Report skill 的完成驗證與異常回報規則。
  - 原始營運報表 prompt 的自動化與實機回歸驗證。
- Out:
  - 放寬個資、帳戶及付款資料安全邊界。
  - 支援任意 SQL literal、HTML 或程式碼 widget。
  - 多報表、持久化、發布與分享功能。
  - 更換 LLM provider 或模型。
  - 新增 Report Canvas 完成狀態 UI；本輪以實際 report state 與 Agent 回覆一致為準。

## Files

- `src/app/webmcp/tool-error.ts`：新增跨 realm 安全錯誤契約與正規化。
- `src/app/webmcp/tool-error.test.ts`：驗證錯誤分類、fallback 與資料遮蔽。
- `src/app/webmcp/completion-policy.ts`：解析頁面宣告的唯讀 completion verifier，並計算下一個 Agent step 的驗證要求。
- `src/app/webmcp/completion-policy.test.ts`：驗證 verifier 安全條件、mutation 後強制驗證與 iframe turn 隔離。
- `src/app/webmcp/session.ts`：讓 Agent-facing tool failure 成為真正的 tool error，並把安全的 verifier annotations 放入當回合快照。
- `src/app/webmcp/session.test.ts`：覆蓋 iframe、fallback provider、跨 realm error 與 completion policy snapshot。
- `src/app/webmcp/tool-debug.ts`：提供可共用的安全 debug sanitizer，並記錄 WebMCP tool input、response 與 error。
- `src/app/webmcp/tool-debug.test.ts`：驗證 input、response、error 及 lifecycle log 不洩漏敏感值。
- `src/app/webmcp/reporting/reporting-tools.ts`：拆分 SQL 安全拒絕原因。
- `src/app/webmcp/reporting/reporting-tools.test.ts`：驗證合法低庫存 SQL及各拒絕分類。
- `src/app/webmcp/reporting/report-tools.ts`：改善 schema、argument error，並為報表 mutation 宣告唯讀 completion verifier。
- `src/app/webmcp/reporting/report-tools.test.ts`：驗證正確及錯誤 widget 參數與 verifier annotations。
- `src/app/webmcp/agent.ts`：加入 step budget、通用 verifier enforcement、最終摘要 step 與 lifecycle log。
- `src/app/webmcp/agent.test.ts`：驗證 mutation 後強制 verifier、最終 step 禁用工具及完成回報 instructions。
- `src/app/webmcp/voltage-admin-skills.ts`：加入報表驗證及部分失敗回報規則。
- `src/app/webmcp/voltage-admin-skills.test.ts`：驗證 report-authoring skill 契約。
- `src/app/webmcp/reporting/smart-dashboard-flow.test.ts`：覆蓋完整報表垂直流程。
- `docs/SMART-DASHBOARD.md`：補充錯誤恢復與完成判定規則。
- `issues/SMART-DASHBOARD-REPORT-CREATION-FAILURE.md`：驗證完成後記錄修正結果。

## Plan

- [x] 1. 建立跨 iframe WebMCP error contract
    Depends on: 無
    - [x] 定義 `WebMcpToolExecutionError`，保存安全的 `toolName`、`category`、`message` 與 `retryable`。
    - [x] 以結構判斷取代單獨的 `instanceof Error`，支援 iframe 建立的 Error-like object。
    - [x] 對未知例外使用固定 generic message，不序列化 stack、任意物件或敏感內容。
    - [x] 讓 Agent-facing iframe tool 在 provider 拋出例外時繼續拋出正規化錯誤，使 AI SDK 產生 `tool-error`。
    - [x] 在 Agent 可見的 error message 中包含安全 category，避免 AI SDK 轉成文字時遺失分類。
    - [x] 分別處理 host-only navigation、特殊 prompt tool 與 abort，避免錯誤契約改變既有 iframe lifecycle。
    - [x] 保留目前尚未提交的 `session.ts` 修改，避免以格式化方式覆蓋使用者變更。
      Verify:
        Tier: 1
        Max attempts: 3
        Check: `npm test -- src/app/webmcp/tool-error.test.ts src/app/webmcp/session.test.ts src/app/webmcp/tool-debug.test.ts`
        Expected: 跨 realm 的 `REPORT_ARGUMENT_ERROR` 不再退化成普通 `{ status: "ERROR" }`；AI SDK tool executor 會 reject 帶安全分類的錯誤，未知例外仍只顯示 generic message。
        Verification log:
          - [1/3] 測試通過（2026-08-28 10:21:34 +08:00）：獨立 verifier 執行指定命令，3 個 test files、34 項 tests 全數通過；確認 cross-realm category 會成為 reject 的 `WebMcpToolExecutionError`、未知例外只使用 generic message，且 special-context、abort 與 host-only navigation lifecycle 均有回歸測試。獨立 implementation reviewer 最終回報 no findings。

- [x] 2. 讓 SQL 與 Report argument errors 可安全修正
    Depends on: `WebMcpToolExecutionError` 提供 Agent-facing category 與 message
    - [x] 將 SQL input guard 拆成敏感欄位、敏感值、疑似識別碼及未核准 literal 等獨立分類。
    - [x] 為每一分類提供不回顯原始資料、但能指引 Agent 修正查詢的訊息。
    - [x] 保持 output privacy guard 與 executor allowlist，不因 demo 查詢失敗而放寬個資限制。
    - [x] 新增原始低庫存需求的合法參數化 SQL 測試，確認 `stock <= 12`、products join 與排序可成功取得 `queryId`。
    - [x] 保留目前已完成的 report display text、結構 ID 與敏感值分組驗證。
    - [x] 改善 report tool schema description，明確說明 `add_report_widget` root 只能包含 `widget`。
    - [x] 讓 unsupported fields error 列出該工具允許的欄位名稱，但不得回顯傳入值。
    - [x] 補上 flattened widget、額外 `reportId`、錯誤 batch 欄位及正確 nested widget 的回歸測試。
      Verify:
        Tier: 1
        Max attempts: 3
        Check: `npm test -- src/app/webmcp/reporting/reporting-tools.test.ts src/app/webmcp/reporting/report-tools.test.ts`
        Expected: 合法低庫存查詢成功；各安全拒絕原因有不同 category；錯誤的 report arguments 回傳可修正提示，正確 KPI、bar、table、text widget 不受影響。
        Verification log:
          - [1/3] 測試失敗（2026-08-28）：80 項中 4 項失敗。三個 computed-result fixture 先被新的 input guard 分類為 `SQL_LITERAL_ERROR`／`SQL_IDENTIFIER_ERROR`，未抵達 output guard；`REPORT_CREATE_ARGUMENT_ERROR` canonical message 使用 `title, audience, and period`，與測試期待的逗號字串不一致。分類：前者為 `STALE_TEST_EXPECTATION`／fixture 未隔離 output guard，後者為 `STALE_TEST_EXPECTATION`；正式安全邊界不放寬。
          - [2/3] 測試通過（2026-08-28）：獨立 verifier 執行指定命令，2 個 test files、98 項 tests 全數通過。真實 SQLite 低庫存 join／排序、controller `queryId` 回讀、四類 SQL input error、獨立 output privacy category、Report root allowed fields 與 KPI／bar／table／text widgets 均符合預期；implementation reviewer 最終 PASS，驗證前後 tracked 狀態一致。

- [x] 3. 以通用 completion policy 確保報表狀態經驗證後才進入最終摘要
    Depends on: Agent 可收到真正的 `tool-error`
    - [x] 將 `sanitizeDebugValue` 提升為明確匯出的共用 sanitizer，供 tool logs 與 Agent lifecycle logs 使用相同遮蔽規則。
    - [x] 定義 iframe tool annotation `completionVerifier`；report mutation tools 指向 `get_report_state`，不在 Agent core 硬編碼 Admin tool names。差異註記：原生 WebMCP WebIDL 不保留未知 annotation，因此 fallback provider 使用 annotation，native discovery 同步使用可保留的 `x-webmcp-completion-verifier` JSON Schema extension。
    - [x] `WebMcpSession.prepareTurn()` 只接受同一回合已 discovery、`readOnlyHint: true`、不需要輸入參數的 verifier，其他 annotation 一律忽略。
    - [x] 將通過驗證的 mutation → verifier 關係放入 immutable `PreparedWebMcpTurn`，網站切換後不得沿用舊 policy。
    - [x] 在 `prepareStep` 檢查上一個完成 step：只要有成功的 annotated mutation，下一個 step 就限制為對應 verifier；同一 step 的平行 verifier 不算 mutation 後驗證。
    - [x] verifier 成功後才解除該 state group 的 pending verification；verifier 失敗時保留 unverified 結果供最終摘要回報，不重試成無限循環。
    - [x] 將 step budget 抽成具名常數，為正常 tool steps、必要 verifier 與最後的 no-tool summary 保留明確容量。
    - [x] 在保留的最終 summary step 設定 `toolChoice: "none"`；若 state 仍未驗證，instructions 必須要求回覆 `PARTIALLY_COMPLETED` 或 `FAILED`，不得回覆 `COMPLETED`。
    - [x] 使用 `onStepEnd` 記錄 DEV-only lifecycle log，包括 step number、finish reason、tool names、tool result/error 類型、pending verifier 與是否進入最終摘要。
    - [x] lifecycle log 不記錄未遮蔽的完整 prompt、tool arguments 或敏感資料。
    - [x] 更新共用 Agent instructions：tool error 代表操作未完成；不得把「將建立」描述成「已建立」。
    - [x] 更新 `voltage-report-authoring` skill：SQL 與 `create_report` 沒有固定先後，但 `add_report_widget` 需要 active report 與有效 `queryId`；完成狀態以 verifier 的最新 report state 為準。
      Verify:
        Tier: 1
        Max attempts: 3
        Check: `npm test -- src/app/webmcp/completion-policy.test.ts src/app/webmcp/agent.test.ts src/app/webmcp/session.test.ts src/app/webmcp/tool-debug.test.ts src/app/webmcp/reporting/report-tools.test.ts src/app/webmcp/voltage-admin-skills.test.ts src/app/webmcp/commerce-safety.test.ts`
        Expected: 每次成功的 annotated report mutation 後，下一個 Agent step 都只能呼叫同一 iframe turn 的 `get_report_state`；未標唯讀、需要參數、跨 turn 或失敗的 verifier 不會被強制執行。最終 no-tool step 能看到 verifier 結果，未驗證或失敗時不得宣稱完成，且所有 logs 維持資料遮蔽。
        Verification log:
          - [1/3] 測試通過但後續失效（2026-08-28）：獨立 verifier 執行指定命令，7 個 test files、111 項 tests 全數通過；其後主代理依計畫檔案清單發現 `report-tools.test.ts` 尚未直接鎖定 provider mutation definitions 的 fallback annotation 與 native schema extension，分類為 `MISSING_TEST_COVERAGE` 並補測，因此本輪結果依規則失效，須重新驗證。
          - [2/3] 測試通過（2026-08-28）：全新獨立 verifier 執行指定命令，7 個 test files、111 項 tests 全數通過。確認 native schema extension 與 fallback annotation、同回合 immutable verifier map、mutation 後強制驗證、same-step verifier 無效、failed verifier 不無限重試、12-step budget、final no-tool status 規則、early final lifecycle log 與資料遮蔽均符合預期；驗證前後 tracked 狀態一致。

- [ ] 4. 建立完整 Smart Dashboard 工作流回歸測試
    Depends on: 可修正 tool errors、最終摘要契約與 report completion 規則
    - [ ] 使用 2026-08-21 至 2026-08-27、`Asia/Taipei` 與低庫存門檻 12 建立固定測試案例。
    - [ ] 覆蓋 dataset status、總營收、前三分類及低庫存三組查詢。
    - [ ] 使用實際 `queryId` 建立 KPI、bar、table 與 evidence text。
    - [ ] 呼叫 `get_report_state` 並斷言四種 widget、期間、時區及 evidence 關係完整。
    - [ ] 分別測試「先 SQL 後 create」與「先 create 後 SQL」，證明兩種順序都成立。
    - [ ] 加入一個 widget 建立失敗案例，確認 report state 是部分完成而非錯誤地標記完整。
      Verify:
        Tier: 1
        Max attempts: 3
        Check: `npm test -- src/app/webmcp/reporting/smart-dashboard-flow.test.ts`
        Expected: 兩種 SQL/create 順序都建立相同完整報表；故意失敗時 report state 準確保留成功項目並暴露缺失 widget。
        Verification log:

- [ ] 5. 使用原始 prompt 實機驗收並收斂文件
    Depends on: 完整工作流回歸測試與 annotation-driven completion verifier 通過
    - [ ] 啟動 Vite 與目前設定的本地 LLM，在 `/chat` 選擇 Voltage Market Admin。
    - [ ] 原樣送出 issue 記錄的營運報表 prompt，不手動修正模型 arguments。
    - [ ] 檢查 tool logs 可按 `callId` 對應 input、response/error，且沒有未遮蔽敏感資料。
    - [ ] 檢查 lifecycle logs 能判斷每一 step、最後 tool results、強制 verifier 與 final summary 的關係。
    - [ ] 驗證每次成功 report mutation 後均出現同一 iframe turn 的 `get_report_state`，且 verifier 發生在下一次可產生完成訊息的 step 之前。
    - [ ] 驗證 Canvas 實際具有 KPI、bar、table、text；若任一項失敗或 verifier 未成功，Agent 必須明確回報部分完成或失敗。
    - [ ] 驗證最後回覆引用最新 `get_report_state`，而不是 tool call 前的預告文字。
    - [ ] 在執行完整品質指令前記錄既有基線：`session.ts` 未使用 debug 參數、`workspace.tsx` 未使用 navigation 變數與 TanStack Virtual warning；執行後逐項判定是否由本次修改造成。
    - [ ] 執行完整測試、typecheck、lint、build 與 architecture boundary 檢查。
    - [ ] 更新 Smart Dashboard 文件的錯誤處理與 completion verifier 協議。
    - [ ] 將 issue 補上根因、測試證據與修正狀態；只有完整驗收後才標記 Closed。
      Verify:
        Tier: 2
        Max attempts: 3
        Check: 依 issue 中相同 prompt 在 `/chat` 完成一次實機操作，確認 lifecycle log 中每次成功 report mutation 的下一個 step 都是 `get_report_state`，再執行 `npm test`、`npm run typecheck`、`npm run lint`、`npm run build`、`npm run check:architecture`。
        Expected: 完整情況下，最新 verifier state 證明 Canvas 存在四種 widget，Agent 才回覆 `COMPLETED`；任何必要操作或 verifier 失敗時回覆 `PARTIALLY_COMPLETED` 或 `FAILED` 並列出缺失項目。品質指令的新失敗為零，既有基線失敗有獨立歸因證據。
        Verification log:

## Open questions

- 無。

---

此計畫基於目前已知資訊產生。若後續 debug arguments 顯示不同的失敗形態，應重新產生或更新受影響的 checkpoints。
