# Plan

將 Chat Room 改為「持久化完整訊息、記憶體暫存 assistant 串流」的資料生命週期，並以已存在的 `[threadId+id]` 索引提供 TanStack Virtual 所需的排序 ID 清單。開啟長對話時，React 與 virtual list 不會先載入所有 `UIMessage` 內容；使用者訊息送出即保存，未完成的 assistant 回覆則不會污染 IndexedDB 或下一輪模型 context。完整歷史只在送出模型請求時由 transport 載入。

> 本計畫請使用 execute-plan 技能執行。Checkbox（`- [ ]`）語法為追蹤格式。

## Scope

- In:
  - 使用 `@tanstack/react-virtual` 改造 Chat Room 訊息渲染。
  - 維持 `messages` 為唯一持久化來源，以既有 `[threadId+id]` 讀取排序後的 primary keys。
  - 開啟 thread 時只載入 message IDs 與目前 runtime overlay；不得將完整 persisted history 初始化給 `useChat` 或 virtual list。
  - user message 送出即寫入 IndexedDB。
  - assistant 串流只存在記憶體；正常完成才持久化。
  - 只有送出模型請求時，才由 transport 載入完整 persisted history 作為模型 context，並與本輪 live messages 合併。
  - stop、error、disconnect、切換 thread 時捨棄未完成 assistant，並排除下一輪模型 context。
  - 以單元測試與瀏覽器操作驗證資料生命週期、捲動及 persistence 行為。

- Out:
  - 新增 `session_message_index`／`thread_message_index` table。
  - IndexedDB 舊資料 schema migration。
  - 歷史訊息分頁、LLM context 壓縮或 token budget 政策。
  - 重新設計訊息 UI、WebMCP iframe 或模型設定。

## Files

- `src/app/assistant/chat-store.ts`：提供訊息 ID 清單、單筆訊息讀取及單筆完整訊息持久化操作。
- `src/app/assistant/chat-message-state.ts`：新增純函式，管理 persisted/live row 合併、去重與 aborted assistant 排除。
- `src/app/assistant/chat-message-state.test.ts`：驗證訊息生命週期與 row 資料模型。
- `src/app/assistant/index.tsx`：讓 `useChat` 僅管理 runtime overlay，立即保存 user、正常完成保存 assistant，以及取消時清除暫存 context。
- `src/app/assistant/chat-window.tsx`：以 TanStack Virtual 呈現 persisted message ID rows 與 temp message rows。
- `src/components/ui/scroll-button.tsx`：改為接受 Virtualizer 的「是否在底部」與「跳到最新」能力，不再依賴 `use-stick-to-bottom` context。
- `src/app/webmcp/transport.ts`：接收由 Chat Session 注入的歷史訊息讀取能力，在送出前載入完整 history，與本輪 live messages 合併並依 message ID 去重。
- `src/app/types.ts`、`AGENTS.md`、`design/data-model.md`：記錄訊息持久化與暫存 assistant 的資料契約。
- `package.json`、`package-lock.json`：移除不再使用的 `use-stick-to-bottom`。

## Plan

- [x] 1. 建立訊息資料讀取與生命週期契約，且不變更 IndexedDB schema。
    Depends on: 無
    - [x] 在 `chat-store.ts` 新增依既有 `[threadId+id]` 索引取得排序 message IDs 的讀取能力；不得先讀取每個 `StoredMessage.message`。
    - [x] 新增依 ID 取得單筆 `StoredMessage`、建立完整 user message、保存完成 assistant message 的操作；每次持久化都同步更新 thread 與 site last-thread 時間。
    - [x] 保留完整歷史訊息讀取能力，供送出模型前建立 conversation context 使用；這個能力不得被 ChatPage 的初始 render 呼叫。
    - [x] 在 `chat-message-state.ts` 定義 persisted IDs、live messages 與 discarded assistant IDs 的合併／去重規則。
    - [x] 明確定義不變量：只有完整 user 或正常完成的 assistant 可落盤；暫存 assistant ID 不得進入持久化清單。
    - [x] 更新資料模型文件與專案執行期規則。
      Verify:
        Tier: 1
        Max attempts: 3
        Check: `npm run test -- chat-message-state`
        Expected: 測試覆蓋 user 立即持久化、完成 assistant 僅出現一次、aborted/error assistant 被排除且不移除 user，以及 persisted/live IDs 去重與順序保持；測試不需要建立新的 IndexedDB table。
        Verification log:
          [1/3] 測試通過: `npm run test -- chat-message-state`，1 個 test file、4 個 tests 全數通過。

- [x] 2. 將 `useChat` 與 transport 改為完整回覆才提交 assistant。
    Depends on: checkpoint 1 的單筆持久化操作、訊息合併規則與完整歷史 loader。
    - [x] 在 thread 開啟時不把 `listChatMessages()` 的完整 records 傳給 `useChat`；`useChat` 初始 state 只包含當前 runtime overlay（首次載入為空），persisted rows 由 message ID 清單與單筆 row query 呈現。
    - [x] 在送出 user 前產生穩定 ULID，先保存與該 ID 對應的完整 user message，再使用相同 message ID 呼叫 `sendMessage`。
    - [x] 將 `useChat` 的完成 callback 改為使用完整 finish event；僅在非 abort、非 disconnect、非 error 時保存 assistant message。
    - [x] 包裝取消與錯誤處理：移除未完成 assistant 的畫面列與 `useChat` in-memory messages，避免它被下一輪送進模型；已保存的 user message 必須保留。
    - [x] 讓切換網站、New Thread、元件卸載沿用同一個取消／捨棄規則。
    - [x] 將 `loadHistory(threadId)` callback 注入 `WebMcpChatTransport`；此 loader 只在送出模型請求時呼叫，transport 再把完整 persisted history 與本輪未捨棄的 live messages 合併，依 message ID 去重後才驗證與送進 Agent。
    - [x] 保持 transport 不直接依賴 Dexie 或 assistant 資料庫實作。
      Verify:
        Tier: 1
        Max attempts: 3
        Check: `npm run test && npm run typecheck && npm run lint`
        Expected: 新增的生命週期測試全數通過；TypeScript 與 ESLint 無錯誤；thread 初始 render 不呼叫完整 history loader；取消後的 assistant 不會出現在下一次 transport 的輸入集合，而已保存 user 仍會存在。
        Verification log:
          [1/3] 測試失敗: PowerShell 不支援計畫 Check 中的 `&&` 連接語法，指令在 shell parse 階段中止，尚未執行 test/typecheck/lint；歸類為 TEST_INFRA_DEFECT。
          [2/3] 測試失敗: `npm run test` 與 `npm run typecheck` 通過，但 `npm run lint` 在 `src/app/assistant/index.tsx:109` 報 React refs 不得於 render 階段寫入；歸類為 CAUSED_BY_CURRENT_CHANGE。
          [3/3] 測試通過: 以 PowerShell 等價分段執行 `npm run test`、`npm run typecheck`、`npm run lint`；3 個 test files、10 個 tests 通過，typecheck 與 lint 通過。原始 `&&` 僅受 shell 語法限制，未改變驗收條件。

- [x] 3. 以 TanStack Virtual 取代 `StickToBottom`，並保留既有訊息視覺與可操作性。
    Depends on: checkpoint 1 的 persisted ID／live row 合併規則，以及 checkpoint 2 的 runtime／discarded message 狀態。
    - [x] 在 `chat-window.tsx` 建立固定高度的 scroll container、Virtualizer 內層總高度容器及絕對定位 message rows。
    - [x] 設定穩定 `getItemKey`、`anchorTo: "end"`、`followOnAppend`、合理的 `scrollEndThreshold` 與 `overscan`。
    - [x] 每個 row 以獨立元件讀取 persisted message，或直接使用 live temp message；不得在 virtual items 的迴圈內直接呼叫 hook。
    - [x] 對 Markdown、圖片、tool details 與 streaming 文字的外層列使用 `measureElement`，讓內容高度變動可重新量測。
    - [x] 將 ScrollButton 改接 `virtualizer.isAtEnd()` 與 `scrollToEnd()`；移除 `StickToBottom` 與其 context 耦合。
    - [x] 保留 `role="log"`、現有 Copy／Speak controls 與訊息時間顯示；virtual row 的 `<details>` 狀態採卸載後不保留，並以 row key／動態高度操作驗證。
      Verify:
        Tier: 2
        Max attempts: 3
        Check: 啟動 `npm run dev`，開啟 `/chat`；以有效本機 LLM 設定送出一則會串流的訊息，在串流中分別停留底端與向上捲動，最後點擊 Cancel。
        Expected: 開啟既有長 thread 時只依 message IDs 渲染可見 rows；底端時串流高度增加仍保持最新位置；向上閱讀時不被拉回；Jump-to-latest 可回到尾端；Cancel 後畫面與重整後都不顯示 partial assistant。
        Verification log:
          [1/3] 測試失敗: 為延長 request 而加入的 Playwright route 使 browser session 非預期關閉，無法取得操作結果；歸類為 OUT_OF_SCOPE（測試工具 session failure，非應用程式輸出）。
          [2/3] 驗證通過但有範圍外失敗: 使用長輸出 prompt 觸發自然 streaming，並在 request 未完成時點擊 Cancel；長 thread 只渲染約 11 個 visible rows、初始在尾端、向上捲動後 Jump-to-latest 可回到底端，取消後 user 保留、partial assistant 不存在且 Cancel button 消失。Playwright 仍記錄 AI SDK abort console stack 與 favicon 404，未影響 UI 或 IndexedDB 判準。

- [ ] 4. 驗證正常持久化、取消捨棄與既有 WebMCP 行為沒有回歸。
    Depends on: checkpoint 2 的完成／取消契約與 checkpoint 3 的 Virtualizer UI。
    - [ ] 正常完成一輪回覆後重整頁面，確認 user 與完整 assistant 都從 IndexedDB 還原，但初始 `useChat` 不會因此持有整段 persisted history。
    - [ ] 在 assistant 串流期間切換網站或建立 New Thread，重開原 thread，確認 user 保留且 partial assistant 不存在。
    - [ ] 驗證 Cinder Studio 與 Field Market 都能完成 iframe tool discovery 與工具執行，確保 Chat UI 重構未跨越 WebMCP runtime 邊界。
    - [ ] 執行 production build，檢查 diff，確認未新增 schema table 或將 temp assistant 寫入持久化層。
      Verify:
        Tier: 2
        Max attempts: 3
        Check: 執行 `npm run build`，並在瀏覽器完成「正常回覆重整」、「取消／切站重整」與兩個 demo 網站 tool discovery 操作。
        Expected: build 成功；完成的訊息可還原；未完成 assistant 永遠不可還原；兩個 iframe 網站各自只顯示並執行自己的 tools；開啟 thread 的畫面不需先讀取所有 message content。
        Verification log:

## Open questions

- 無。本計畫採用既有 `[threadId+id]` 索引取得 primary keys，不新增 `session_message_index` table；若實測顯示 ID 清單讀取本身是瓶頸，再以量測結果建立後續 materialized index 決策。

---

此計畫基於目前已知資訊產生。若有新資訊或需求變更，請要求重新產生計畫。
