# Plan

修正實機測試發現的 WebMCP 導覽同步、Returns 工具串接與商品草稿阻塞問題，並補齊退款流程 fixture。完成後，Agent 能可靠地跨頁操作安全工具，使用者可恢復草稿，且下一輪可驗證退款計算與核准詳情的正常流程。

> 本計畫請使用 execute-plan 技能執行。Checkbox（`- [ ]`）語法為追蹤格式。

## Scope

- In:
  - route-aware WebMCP 工具同步與 Agent 提醒。
  - `search_returns` 與後續 RMA 工具的 identifier 契約一致性。
  - 可恢復、自動保存的商品編輯草稿。
  - RMA calculation、pending／returned／pending-execution approval fixture。
  - 單元、workflow 與 Codex 內建瀏覽器實機驗證。

- Out:
  - Agent 執行發布、資格決定、驗貨、退款核准或退款。
  - 放寬任何個資、付款資料與安全 SQL 邊界。
  - 重構未受本次測試影響的 Orders、Inventory、Customers 或 Reports 功能。

## Files

- `src/app/webmcp/voltage-admin.tsx`：調整 route tool 註冊與就緒生命週期。
- `src/app/webmcp/voltage-admin-skills.ts`：加入 Agent 可見的導覽後重新 discovery 提醒。
- `src/app/webmcp/voltage-admin-skills.test.ts`：驗證提醒存在於 `agent_instructions.text`。
- `src/app/webmcp/returns/return-tools.ts`：統一 Returns 搜尋結果與詳情工具的 RMA identifier。
- `src/app/webmcp/returns/return-seed.ts`：建立完整且一致的退款流程 fixture。
- `src/app/webmcp/returns/return-repository.ts`：提升 seed version，讓既有 IndexedDB 補入新 fixture。
- `src/app/webmcp/returns/*.test.ts(x)`：驗證 fixture、一致 identifier 與退款安全投影。
- `src/app/webmcp/products/product-editor.tsx`：改為非阻塞的可恢復草稿 UI。
- `src/app/webmcp/products/product-editor-controller.ts`：支援草稿恢復狀態。
- `src/app/webmcp/products/product-draft-store.ts`：新增持久化 editor draft store。
- `src/app/webmcp/products/*draft*.test.ts(x)`：驗證自動保存、恢復與使用者捨棄。

## Plan

- [x] 1. 穩定 route-aware WebMCP 工具生命週期
    Depends on: 無

    - [x] 將 route 切換時的 tool 撤銷、註冊與 ready signal 視為單一生命週期，確保 fallback provider 只在新工具集合完整可 discovery 後宣告 ready。
    - [x] 為 fallback provider 加入 route 切換後重新 discovery 的回歸測試，涵蓋全域頁、detail route 與 editor route；不以固定延遲作為正確性依據。
    - [x] 在 `getVoltageAdminAgentInstructions()` 的回傳文字加入「導覽後不可沿用上一頁工具，須重新 discovery」提醒。
    - [x] 將 native `document.modelContext` 的 `toolchange` 與立即重新 discovery 行為保留給 checkpoint 5 的 Codex 內建瀏覽器實機驗證；mock event 不得作為原生行為證據。

      Verify:
        Tier: 1
        Max attempts: 3
        Check: `npm run test -- src/app/webmcp/voltage-admin-skills.test.ts src/app/webmcp/products/product-tools-workflow.test.tsx src/app/webmcp/returns/return-tools-workflow.test.tsx`
        Expected: Agent instructions 含重新 discovery 限制；fallback provider 在每次 route 變化完成後只回傳目前頁可用工具，且不需依賴固定等待時間。原生 toolchange 不在此自動測試的驗收範圍。
        Verification log:
        - [1/3] PASS — 2026-08-31：`npm run test -- src/app/webmcp/voltage-admin-skills.test.ts src/app/webmcp/products/product-tools-workflow.test.tsx src/app/webmcp/returns/return-tools-workflow.test.tsx`，3 個檔案、25 項測試通過。獨立 reviewer 與 verifier 均確認 `agent_instructions` 包含導覽後重新 discovery 提醒，fallback route lifecycle 以 ready Promise 與 cleanup 管理，workflow 使用條件式等待而非固定延遲；verifier 確認 git status 前後相同。

- [x] 2. 修正 Returns 跨工具 identifier 契約
    Depends on: `search_returns` result 的 `rmaId` 欄位可供 detail/navigation tools 使用

    - [x] 讓 `search_returns` 每筆結果輸出 `rmaId`；若需相容既有 UI，保留 `id` 但明確標示兩者相同。
    - [x] 更新 tool description 與 schema 說明，使「搜尋 → 詳情／導覽」無需 Agent 自行推測欄位。
    - [x] 加入 workflow 測試：將搜尋結果的 `rmaId` 原樣帶入 `get_return_detail` 與 `open_return_detail`。

      Verify:
        Tier: 1
        Max attempts: 3
        Check: `npm run test -- src/app/webmcp/returns/return-tools.test.ts src/app/webmcp/returns/return-tools-workflow.test.tsx`
        Expected: 搜尋結果可直接串接 RMA 詳情與導覽工具；輸出不含個資、付款資料或私密備註。
        Verification log:
        - [1/3] PASS — 2026-08-31：`npm run test -- src/app/webmcp/returns/return-tools.test.ts src/app/webmcp/returns/return-tools-workflow.test.tsx`，2 個檔案、11 項測試通過。獨立 reviewer 確認 `rmaId`／相容 `id` 的契約、closed schema 與安全投影；獨立 verifier 重跑測試通過，並確認實作未寫入工作樹。

- [x] 3. 補齊一致且可重測的退款流程 fixture
    Depends on: Returns Repository 的 RMA、item、calculation、approval、timeline 資料模型

    - [x] 保留既有的 authorized 與 needs-information RMA，並新增已完成驗貨且存在有效 calculation 的 RMA。
    - [x] 新增至少三筆 approval fixture：`pending`、`returned`、`approved + pending_execution`。
    - [x] 每筆 fixture 使用不同可退款訂單品項，並建立相符 calculation version、approval status、refund status 與 timeline。
    - [x] 以既有退款計算邏輯建立金額，不手寫違反領域規則的金額。
    - [x] 提升 `RETURN_SEED_VERSION`，確保既有 IndexedDB 補入固定新資料而不覆寫使用者既有資料。
    - [x] 為 calculation 正常讀取、approval 清單篩選、approval 詳情與 immutable calculation 重讀增加測試。

      Verify:
        Tier: 1
        Max attempts: 3
        Check: `npm run test -- src/app/webmcp/returns/return-seed.test.ts src/app/webmcp/returns/return-repository.test.ts src/app/webmcp/returns/return-tools.test.ts src/app/webmcp/returns/refund-approval-list-model.test.ts`
        Expected: 至少一筆有效 calculation 可讀取；pending、returned、pending-execution 核准單均可安全查詢與導覽；不會出現重複退款或敏感資料。
        Verification log:
        - [1/3] PASS — 2026-08-31：4 個指定測試檔、32 項測試通過。獨立 review 補強後確認 migration、immutable-read 與 pending／returned／approved 的版本語意；verifier 確認不同 order line、`calculateRefund`、只補缺 migration 與安全投影皆符合驗收。

- [x] 4. 實作可恢復的商品 editor 草稿，移除離頁阻塞
    Depends on: 持久化 editor draft store 與草稿恢復狀態

    - [x] 新增獨立的 IndexedDB editor draft store，不直接將未完成新增品寫入正式 Product Repository。
    - [x] 在 Agent 或使用者修改 editor draft 後自動保存；重新進入相同新增／編輯頁時偵測可恢復草稿。
    - [x] 以頁面內可見 UI 提供「繼續草稿」與「捨棄草稿」；僅使用者可捨棄。
    - [x] 移除內部 route 導覽對 `unstable_usePrompt` 的阻塞依賴；如仍保留瀏覽器關閉保護，須以已持久化草稿為前提。
    - [x] 擴充 `get_product_editor_state` 的安全狀態，使 Agent 可驗證草稿已保存／已恢復，但不可刪除草稿。

      Verify:
        Tier: 1
        Max attempts: 3
        Check: `npm run test -- src/app/webmcp/products/product-editor.test.tsx src/app/webmcp/products/product-tools.test.ts src/app/webmcp/products/product-tools-workflow.test.tsx`
        Expected: 套用可逆草稿後可離開 editor 而無原生 discard confirm；重新進入時能恢復相同草稿；使用者可在 UI 捨棄，Agent 沒有捨棄工具。
        Verification log:
        - [1/3] PASS — 2026-08-31：3 個指定測試檔、21 項測試與 `npm run typecheck` 通過。獨立 reviewer／verifier 均確認草稿存於獨立 Dexie store、UI-only discard、無 `unstable_usePrompt`／`beforeunload`、安全 persistence 狀態與 stale callback guard；內建瀏覽器另確認 autosave 後離頁再進入回復成功。

- [x] 5. 執行全量品質檢查與第二輪內建瀏覽器實機驗證
    Depends on: checkpoints 1–4 的 route tools、fixture 與可恢復草稿

    - [x] 執行全量 test、typecheck、lint 與 build。
    - [x] 在已登入的 Codex 內建瀏覽器確認：native toolchange 後立即重新 discovery、Returns 搜尋到詳情直接串接、商品草稿離頁再進入、退款 calculation／approval 三種 fixture。
    - [x] 將每項實機結果與未預期差異追加至 WebMCP 測試紀錄。

      Verify:
        Tier: 2
        Max attempts: 3
        Check: 執行 `npm run test`、`npm run typecheck`、`npm run lint`、`npm run build`，再以 `npm run dev` 與 Codex 內建瀏覽器完成上述四條實機鏈路。
        Expected: 品質指令通過；內建瀏覽器中 native toolchange 後的 discovery 不會保留前頁工具，且沒有 RMA identifier 串接失敗或草稿阻塞；退款計算與核准詳情能以 fixture 實測。
        Verification log:
        - [1/3] PASS — 2026-08-31：`npm run test` exit 0（69 個測試檔、603 項測試，172.62 秒），`npm run typecheck`、`npm run lint`、`npm run build` 全數通過；build 僅出現既有 bundle-size warning。已登入內建瀏覽器驗證 stale native handle 會拒絕並要求重新 discovery、草稿保存/離頁回復、`rmaId` 搜尋直傳詳情、`CAL-2006` 與三種 approval fixture；結果已追加至 `docs/WEBMCP-AGENT-LIVE-TEST-LOG.md`。獨立 test verifier 亦取得相同的 603/603 exit 0 證據。

## Open questions

- 無。

---

此計畫基於目前已知資訊產生。若有新資訊或需求變更，應重新產生計畫。
