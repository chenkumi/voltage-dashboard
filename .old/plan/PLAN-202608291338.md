# Plan

將現有 Voltage Dashboard 擴充為可展示跨模組行政流程的電商營運自動化平台。保留既有 Smart Dashboard、庫存與報表能力，新增商品上架草稿、營運異常案件、售後建議與人工核准流程，讓 Agent 能完成低風險準備工作，而所有高風險最終操作仍由使用者在頁面內執行。

> 本計畫請使用 execute-plan 技能執行。Checkbox（`- [ ]`）語法為追蹤格式。

## Scope

- In:
  - 新增 Catalog Intake、Operations Cases、Approval Inbox 三個 Outlet 頁面。
  - 建立商品候選資料、商品草稿、訂單異常、退貨案件、審核項目及稽核紀錄。
  - Agent 可搜尋資料、建立／更新草稿、分類案件、判斷退貨資格及產生客服建議。
  - 使用者可在頁面中發布商品草稿、核准處理方案或執行模擬最終操作。
  - 新增對應 WebMCP tools、skills、route-aware instructions 與完成驗證。
  - 修正既有訂單／客戶 tools 可能回傳穩定帳戶識別碼的問題。
  - 保留既有 SQL、Report Canvas、query cache 與 runtime 隔離限制。
  - 使用共用、記憶體內的 seeded demo workspace；跨路由保留，頁面 reload 後重設。

- Out:
  - 真實供應商網站爬取或外部搜尋。
  - 真實商品發布、付款、退款、取消或確認訂單。
  - 個資、地址、帳戶識別及付款資料。
  - 後端、登入、權限角色、多租戶與跨裝置持久化。
  - Storefront、Chat Room、內建 Agent 或多網站 registry。
  - 擴充 SQLite Agent database 的既有四個 curated datasets。
  - 任意 HTML、程式碼 widget 或 Agent 自動提交高風險表單。

## Files

- `src/App.tsx`：註冊三個新 Outlet 路由。
- `src/app/webmcp/voltage-admin.tsx`：整合營運 workflow state、tools executor、導覽與 route-aware instructions。
- `src/app/webmcp/voltage-admin-layout.tsx`：新增模組導覽、待處理數量與作用中狀態。
- `src/app/webmcp/voltage-admin-pages.tsx`：保留既有頁面，改用抽出的共用頁面布局。
- `src/app/webmcp/voltage-admin-page-layout.tsx`：提供符合既有 `PageLayout`、12 欄 grid 與 block padding 規則的共用布局。
- `src/app/webmcp/voltage-admin-data.ts`：建立不含穩定客戶識別碼的安全訂單與客群摘要。
- `src/app/webmcp/voltage-admin-skills.ts`：加入商品上架、異常分類、退貨政策與人工核准 skills。
- `src/app/webmcp/operations/types.ts`：定義商品候選、草稿、案件、資格判斷、審核與稽核模型。
- `src/app/webmcp/operations/operations-data.ts`：提供不含個資及付款資料的固定示範資料。
- `src/app/webmcp/operations/operations-state.ts`：管理 Agent 草稿、人工核准、workflow snapshot 與 audit trail。
- `src/app/webmcp/operations/operations-tools.ts`：定義及執行營運自動化 WebMCP tools。
- `src/app/webmcp/operations/catalog-intake-page.tsx`：商品資料來源、缺漏欄位、草稿與發布前審核。
- `src/app/webmcp/operations/operations-cases-page.tsx`：訂單異常、退貨案件、分類與客服建議。
- `src/app/webmcp/operations/approval-inbox-page.tsx`：人工核准、最終動作及活動紀錄。
- `src/app/webmcp/operations/*.test.ts`：workflow reducer、政策判斷、executor 與 schema 測試。
- `src/app/webmcp/commerce-safety.test.ts`：擴充個資、付款與高風險副作用防護測試。
- `src/app/webmcp/voltage-admin.css`：新增頁面與 responsive 狀態樣式。
- `docs/COMMERCE-AUTOMATION.md`：記錄資料模型、工具契約、安全邊界及 demo 流程。
- `AGENTS.md`：在功能落地後補充 operations 模組入口與新參考文件。

## Plan

- [ ] 1. 建立安全的營運 workflow domain，使三個流程共享同一份可驗證狀態
    Depends on: 無
    - [ ] 在 `operations/types.ts` 定義 `CatalogCandidate`、`ProductDraft`、`OpsCase`、`CaseDraft`、`EligibilityResult`、`ReviewItem`、`AuditEntry` 與 `WorkflowSnapshot`。
    - [ ] 使用非個人、不可連回自然人的 workflow/case ID；訂單異常只保存出貨、付款檢核及地址驗證的狀態碼，不保存實際付款或地址內容。
    - [ ] 在 `operations-data.ts` 建立涵蓋商品上架、未出貨、付款失敗、地址異常及退貨申請的固定資料。
    - [ ] 在 `operations-state.ts` 實作可逆草稿更新、審核佇列、人工最終處理及稽核紀錄；所有 reducer/action 都再次驗證 enum、長度與允許欄位。
    - [ ] 將既有訂單 tool output 移除 `customerId`，將客戶 tool 改為分群彙總，不回傳穩定客戶識別碼。
      Verify:
        Tier: 1
        Max attempts: 3
        Check: `npm run test -- src/app/webmcp/operations/operations-state.test.ts src/app/webmcp/voltage-admin-data.test.ts src/app/webmcp/commerce-safety.test.ts`
        Expected: 草稿與審核狀態轉換可重現；非法 enum、負值、超長文字及額外欄位被拒絕；所有 Agent 可見結果不含姓名、Email、電話、地址、帳戶識別或付款資料。
        Verification log:

- [ ] 2. 建立共用 Outlet 布局與三個營運模組路由
    Depends on: checkpoint 1 的 `WorkflowSnapshot` 與待處理數量
    - [ ] 將 `PageLayout` 與 `GridBlock` 抽至 `voltage-admin-page-layout.tsx`，維持 Header `p-1`、12 欄 grid、`gap-2` 及每個 block `p-1`。
    - [ ] 將 `catalog-intake`、`operations-cases`、`approvals` 加入 `VoltageAdminView`、route parser、`App.tsx` 與導覽列。
    - [ ] 在導覽列顯示待完成商品草稿、待分類案件及待核准項目數量。
    - [ ] 保留 Dashboard、Products、Orders、Customers、Inventory、Reports 的既有路徑與行為。
      Verify:
        Tier: 1
        Max attempts: 3
        Check: `npm run test -- src/app/webmcp/operations/operations-navigation.test.ts && npm run typecheck`
        Expected: 所有新舊路徑均能解析為正確 view，未知路徑仍回到 Dashboard；三個新頁面均使用共用 `PageLayout`，TypeScript 無錯誤。
        Verification log:

- [ ] 3. 完成 Catalog Intake 商品上架草稿流程
    Depends on: checkpoint 1 的 `CatalogCandidate`、`ProductDraft`、`ReviewItem`
    - [ ] 建立商品候選清單，顯示來源名稱、來源時間、原廠規格、缺漏欄位與來源可信度。
    - [ ] 建立商品草稿編輯區，支援標題、分類、描述及受限規格欄位，並顯示 Agent 與使用者最後修改來源。
    - [ ] 將來源文字視為外部不可信內容，只作為資料顯示，不渲染任意 HTML。
    - [ ] 建立發布前預覽及頁面內「發布商品」控制項；發布只可由使用者直接操作，且產生 audit entry。
    - [ ] 發布後同步更新草稿與審核狀態，但不呼叫真實外部服務。
      Verify:
        Tier: 2
        Max attempts: 3
        Check: 啟動 `npm run dev`，在 `/catalog-intake` 依序選擇候選商品、補齊草稿、開啟預覽並由頁面按鈕發布；分別以 375、768、1024、1440 px 檢查版面。
        Expected: 來源、缺漏欄位、草稿及預覽清楚可辨；只有頁面按鈕能完成發布；狀態與 audit trail 立即更新；各 viewport 無 body overflow。
        Verification log:

- [ ] 4. 完成訂單異常與售後案件流程
    Depends on: checkpoint 1 的 `OpsCase`、`CaseDraft`、`EligibilityResult`
    - [ ] 建立 Operations Cases 清單，支援依案件類型、處理狀態與優先級篩選。
    - [ ] 以安全狀態碼呈現未出貨、付款失敗及地址驗證異常，不顯示付款資料或實際地址。
    - [ ] 建立案件分類草稿，保存分類、優先級、證據項目與建議下一步，但不改變訂單本身。
    - [ ] 建立確定性的退貨資格判斷函式，輸出 `eligible`、`ineligible` 或 `needs_human_review`、命中政策及缺漏證據。
    - [ ] 建立客服建議草稿及頁面內最終處理區；退款、取消或訂單變更只能由使用者直接操作。
      Verify:
        Tier: 1
        Max attempts: 3
        Check: `npm run test -- src/app/webmcp/operations/return-policy.test.ts src/app/webmcp/operations/operations-state.test.ts`
        Expected: 固定案件會得到預期資格結果；資料不足必須回傳 `needs_human_review`；Agent 草稿不會改變訂單、付款、退款或取消狀態；人工操作才會完成模擬最終處理。
        Verification log:

- [ ] 5. 完成 Approval Inbox 與跨模組人工核准
    Depends on: checkpoint 3 的商品審核項目及 checkpoint 4 的案件審核項目
    - [ ] 將商品草稿、訂單分類與售後建議彙整到同一個 Approval Inbox。
    - [ ] 每筆審核項目顯示安全摘要、Agent 建議、證據來源、目前狀態及必須由人執行的最終動作。
    - [ ] 實作頁面內核准、退回修改與最終處理按鈕，禁止以 URL、chat confirmation 或 tool input 取代按鈕操作。
    - [ ] 建立不含 prompt、個資及任意輸入內容的 audit trail，只記錄 actor、action、workflow ID、時間與結果類型。
    - [ ] 在 Dashboard 增加商品草稿、異常案件與待核准數量，讓三條流程形成統一營運工作台。
      Verify:
        Tier: 1
        Max attempts: 3
        Check: `npm run test -- src/app/webmcp/operations/approval-flow.test.ts src/app/webmcp/operations/operations-state.test.ts`
        Expected: 商品與案件草稿會進入同一審核佇列；退回修改不執行最終操作；只有 UI actor 可將審核項目標示為已完成；audit trail 不含敏感或自由輸入內容。
        Verification log:

- [ ] 6. 暴露精簡且安全的跨模組 WebMCP 能力
    Depends on: checkpoints 1–5 的 workflow state、頁面與人工審核入口
    - [ ] 在 `operations-tools.ts` 定義 `list_catalog_candidates`、`get_catalog_candidate`、`save_product_draft`、`open_product_review`。
    - [ ] 定義 `list_ops_cases`、`get_ops_case`、`save_case_draft`、`check_return_eligibility`、`open_case_review`。
    - [ ] 定義 `list_pending_reviews` 與無輸入的 `get_workflow_state`；使用後者作為兩個 draft mutation tools 的 completion verifier。
    - [ ] 所有 schema 使用 `additionalProperties: false`；executor 獨立驗證 ID、enum、字串長度、陣列大小與目前 workflow 狀態。
    - [ ] 為候選商品、來源文字及案件外部內容加入 `untrustedContentHint: true`；所有輸出限制在約 1.5K 字元內。
    - [ ] `open_product_review` 與 `open_case_review` 只導覽到審核頁，不發布商品、不退款、不取消、不確認訂單。
    - [ ] 加入 `catalog-onboarding`、`order-exception-triage`、`return-policy`、`approval-boundaries` skills，並讓 `agent_instructions` 依目前 route 說明可用能力與人工步驟。
    - [ ] 保持全站 Provider 可發現跨模組 tools，使 Agent 不需模擬 DOM 點擊即可串接工作流程。
      Verify:
        Tier: 1
        Max attempts: 3
        Check: `npm run test -- src/app/webmcp/operations/operations-tools.test.ts src/app/webmcp/voltage-admin-skills.test.ts src/app/webmcp/completion-policy.test.ts src/app/webmcp/commerce-safety.test.ts`
        Expected: discovery 包含所有指定 tools 與四個 skills；mutation 完成後必須以 `get_workflow_state` 驗證；schema、executor、instructions 與 skills 安全界線一致；不存在發布、付款、退款、建立／確認／取消訂單 tool。
        Verification log:

- [ ] 7. 完成跨模組 demo、回歸驗證與文件
    Depends on: checkpoint 6 的完整 WebMCP tool set 與三個可操作頁面
    - [ ] 建立 fallback provider workflow test，覆蓋商品候選 → 商品草稿 → 人工發布。
    - [ ] 建立訂單異常 → 分類草稿 → 退貨資格 → 客服建議 → 人工核准流程測試。
    - [ ] 驗證 Agent 修改後 UI 立即反映、使用者修改後 `get_workflow_state` 可讀到最新狀態、reload 後不重用舊 workflow context。
    - [ ] 確認既有庫存、唯讀 SQL、Report Canvas、completion verifier 與 query cache 測試仍通過。
    - [ ] 撰寫 `docs/COMMERCE-AUTOMATION.md`，記錄工具契約、資料邊界、人機分工與三分鐘 demo 腳本。
    - [ ] 在原生 WebMCP 環境執行 discovery、tools、skills 與人工核准驗證；若環境仍無原生 API，明確記錄 fallback 與待實機項目，不混淆兩者。
      Verify:
        Tier: 2
        Max attempts: 3
        Check: 先執行 `npm run test`、`npm run typecheck`、`npm run lint`、`npm run build`；再以 production preview 依序完成「商品上架草稿」及「營運異常與售後」兩條 demo，檢查 WebMCP discovery、tool 結果、UI 狀態、人工最終核准與 reload 隔離。
        Expected: 四項專案指令全部成功；兩條 demo 均能從自然語言目標推進到待人工核准；Agent 無法完成高風險最終操作；既有 Dashboard、Inventory、Reports 與 SQL 流程無回歸；原生與 fallback 驗證結果被清楚區分。
        Verification log:

## Open questions

- 無。計畫採用先前討論的三頁結構、精簡跨模組 tools、記憶體內 demo state，以及頁面人工最終核准。

---

此計畫基於目前已知資訊產生。若有新資訊或需求變更，請要求重新產生計畫。
